# app

This folder is responsible only for application wiring.

It contains:

- Routes wiring
- Providers wiring
- Global config
- Global styles

Guidelines:

- Keep this layer thin.
- Do not place business logic here.
- Delegate domain behavior to `features`.
