# shared

This folder contains reusable code shared across multiple features.

Rules:

- No business logic here.
- Keep shared modules generic and reusable.
- Move domain-specific behavior back into the owning feature.
