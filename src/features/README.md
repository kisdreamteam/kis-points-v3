# features

This folder contains business logic using a feature-first architecture.

Rules:
- Each feature owns its own pages, components, hooks, stores, services, and types.
- Keep feature code together instead of scattering by technical layer.
- Prefer symmetry between features for readability and predictability.
